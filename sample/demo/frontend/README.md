# Demo Frontend

This is a simple React frontend for the Spring Boot demo backend.

## Setup

1. Open a terminal in `sample/demo/frontend`
2. Run `npm install`
3. Run `npm run dev`

The app proxies API requests to `http://localhost:8080/api`.
